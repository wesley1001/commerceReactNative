
import {
  setCurrentMenuId,
  setSlideImages,
  setListData,
  pendingListData,
  fetchNewsList,
  fetchCarouselImages,
} from './appActions'

const actions = {
  setCurrentMenuId,
  setSlideImages,
  setListData,
  pendingListData,
  fetchNewsList,
  fetchCarouselImages,
};

export default actions;